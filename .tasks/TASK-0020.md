# TASK-0020

## Título

4 perguntas novas (vistas, folhas, categoria líder, famílias)

## Objetivo

Adicionar consultas de vistas (View, excluindo templates), folhas (ViewSheet), categoria com mais elementos (entre 7 categorias conhecidas) e lista de famílias carregadas (GroupBy Family.Name).

## Status

Aprovado

## Resultado

Implementados handlers para vistas (filtro `!IsTemplate`), folhas, `FindCategoryWithMostElements()` (itera 7 categorias), `ListLoadedFamilies()` (GroupBy, ordenado por nome, resposta multilinha).

## Commit relacionado

`94c9c23` — feat: add views, sheets, top category and family list queries
