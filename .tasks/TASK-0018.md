# TASK-0018

## Título

5 perguntas novas (ambientes, circuitos, área, eletroduto, potência)

## Objetivo

Adicionar consultas de ambientes (Rooms), circuitos elétricos (ElectricalSystem), área construída (ROOM_AREA), comprimento de eletrodutos (CURVE_ELEM_LENGTH) e potência instalada (lookup por nome).

## Status

Aprovado após correção

## Resultado

Implementados handlers inline para ambientes, circuitos, vistas, folhas;
`SumParameter()` reutilizável para área e eletrodutos;
`SumPowerParameter()` com lookup por nome ("Apparent Load Phase A", "Carga Aparente", "Potência") para potência.

## Follow-up

TASK-0018.1: mensagens contextuais adicionadas para resultados zerados (ex.: "Nenhum ambiente encontrado no arquivo local — se o modelo de arquitetura estiver vinculado por link...").

## Commit relacionado

`4f02443` — feat: add contextual warnings for zero-result queries
