# TASK-0022.1

## Título

Correção: parsing manual → JSON deserialization

## Objetivo

Substituir a extração manual da resposta do Ollama (IndexOf/Substring) por `JsonSerializer.Deserialize` com classes `OllamaChatResponse`/`OllamaMessage` tipadas.

## Status

Aprovado

## Resultado

Parsing manual substituído por `JsonSerializer.Deserialize<OllamaChatResponse>()`. Adicionado `System.Text.Json` NuGet (versão 10.0.9, necessário pois o projeto target é net48). Classes DTO com propriedades `Message`, `Role`, `Content`. Tratamento de `JsonException` adicionado.

## Commit relacionado

`92409c0` — fix: use JSON deserialization for Ollama response parsing
