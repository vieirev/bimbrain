# TASK-0022

## Título

Conexão básica com Ollama (qwen3)

## Objetivo

Adicionar botão "Testar IA" que envia mensagem fixa ao Ollama local (http://localhost:11434/api/chat) e exibe a resposta no `_responseText`, provando conectividade básica do plugin com LLM local.

## Status

Aprovado após correção

## Resultado

Botão "Testar IA" adicionado ao painel de entrada. Handler `OnTestIaClick` (async void) usando `HttpClient` com timeout de 30s. Envia JSON com model qwen3 e messages, exibe resposta. Tratamento de erro para timeout, conexão recusada e exceções genéricas com mensagens claras.

## Follow-up

TASK-0022.1: parsing manual de string substituído por `JsonSerializer.Deserialize<OllamaChatResponse>()` com DTOs tipados.

## Commit relacionado

(mesmo commit da TASK-0024: `658c0ef` — a implementação inicial foi consolidada no merge)
