# US-0050 — Engineering Dictionary

## Título

TASK-0074: Primeiro dicionário oficial de engenharia do BIMBrain (Knowledge
Engine + Classification Engine, EPIC-0005).

## Objetivo

Criar e cadastrar o dicionário de aliases de engenharia (`aliases.json`) e os
métodos de leitura em `ClassificationRepository`. Nesta task é apenas
infraestrutura — nenhuma classificação utiliza aliases ainda.

## Critérios de Aceite

- [x] `knowledge/classification/aliases.json` criado.
- [x] `ClassificationRepository` carrega o JSON.
- [x] `GetAliases(type)`, `GetAllAliases()`, `HasAlias(type, string)`,
  `FindByAlias(string)` implementados.
- [x] Nenhuma Rule alterada.
- [x] Nenhuma IA.
- [x] Nenhuma consulta.
- [x] Nenhuma classificação utiliza aliases ainda.
- [x] Apenas infraestrutura.

## Arquivos criados

- `knowledge/classification/aliases.json` — dicionário (Outlet, Switch,
  LightingFixture, Panel, Circuit + aliases).

## Arquivos modificados

- `src/BIMBrain/Knowledge/ClassificationRepository.cs` — carrega `aliases.json`
  (buscando `knowledge` a partir do assembly) e implementa `GetAliases`,
  `GetAllAliases`, `HasAlias`, `FindByAlias` (case-insensitive). `GetAliases`
  anterior (lista vazia) foi substituída pela leitura real.
- `docs/ARCHITECTURE.md` — seção `Engineering Dictionary` (Knowledge Engine).
- `docs/CAPABILITIES.md` — seção `Engineering Dictionary`.
- `docs/US-0050.md` — criado.

## Resumo técnico

- Aliases mapeiam nomes técnicos em português/inglês para `ElementClassificationType`.
- `FindByAlias` resolve o tipo a partir de um alias (ignora caixa); retorna
  `Unknown` se não encontrado.
- Leitura defensiva: arquivo ausente ou JSON inválido → dicionário vazio, sem
  exceções.
- Sem impacto em classificação, regras ou consultas.

## Build

- 0 erros.
