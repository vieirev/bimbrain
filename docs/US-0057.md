# US-0057 — Infraestrutura de Query Handlers

TASK-0081: Criar a infraestrutura oficial de Query Handlers para substituir
gradualmente os grandes blocos condicionais do `QuestionProcessor`. Nenhum
comportamento existente muda.

## Engine

Question Engine

## EPIC

EPIC-0006

## User Story

US-0057

## Objetivo

Estabelecer a camada `Question Engine → Query Router → Query Handlers`, com os
tipos base `IQueryHandler`, `QueryContext`, `QueryResult` e `QueryRouter`.
Nenhum handler concreto é criado nesta task.

## Arquivos criados

- `src/BIMBrain/Queries/IQueryHandler.cs` — `bool CanHandle(QueryContext)` +
  `Task<QueryResult> HandleAsync(QueryContext)`.
- `src/BIMBrain/Queries/QueryContext.cs` — contexto da consulta (`Question`,
  `CopilotContext`, `UIDocument`, `Document`); apenas dados, sem lógica.
- `src/BIMBrain/Queries/QueryResult.cs` — `Success`, `Response`, `Handled`.
- `src/BIMBrain/Queries/QueryRouter.cs` — recebe `IEnumerable<IQueryHandler>`,
  resolve o primeiro `CanHandle() == true`, executa `HandleAsync` e retorna o
  `QueryResult`; se nenhum aceitar, retorna `Handled = false`.

## Arquivos modificados

- `docs/ARCHITECTURE.md` — nova camada Question Engine → Query Router → Query Handlers.
- `docs/CAPABILITIES.md` — nova seção "Query Infrastructure".
- `docs/US-0057.md` — este documento.

## Critérios atendidos

- [x] Nenhuma funcionalidade muda.
- [x] Nenhuma consulta muda.
- [x] Nenhuma Rule muda.
- [x] Nenhuma IA.
- [x] Build: 0 erros.

## Resumo técnico

- Apenas infraestrutura; `QuestionProcessor` permanece intacto e funcionando.
- `QueryRouter.RouteAsync` itera os handlers e delega ao primeiro que aceita a
  consulta; comportamento de fallback (`Handled = false`) definido para quando
  nenhum handler corresponder.
- Nenhum `if` do `QuestionProcessor` foi removido; nenhum handler concreto foi
  implementado.
